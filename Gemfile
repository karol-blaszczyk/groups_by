source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in groups_by.gemspec
gemspec

group :development, :test do
  gem 'awesome_print'
  gem 'colorize'
  gem 'faker'
  gem 'pry'
end
