source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in rGrouper.gemspec
gemspec

group :development, :test do
  gem 'pry'
  gem 'awesome_print'
  gem 'colorize'
  gem 'faker'
end
